# Example .env file for Vortludo

# Set to "release" for production, "development" for local dev
GIN_MODE=development
# ENV=production

# Optionally set the port (default is 8080)
# PORT=8080

# Session/game configuration (optional overrides)
# SESSION_TIMEOUT=2h         # How long a session is valid (e.g. 30m, 2h, 24h)
# COOKIE_MAX_AGE=2h          # How long the session cookie is valid
# STATIC_CACHE_AGE=5m        # How long static assets are cached by browsers

# Rate limiting (requests per second and burst)
# RATE_LIMIT_RPS=5           # Requests per second per client IP
# RATE_LIMIT_BURST=10        # Maximum burst of requests allowed per client IP

# Example: Uncomment and adjust values as needed for your environment
# GIN_MODE=release
# ENV=production
# PORT=8081
# SESSION_TIMEOUT=1h
# COOKIE_MAX_AGE=1h
# STATIC_CACHE_AGE=10m
# RATE_LIMIT_RPS=20
# RATE_LIMIT_BURST=40
